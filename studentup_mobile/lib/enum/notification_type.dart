enum NotificationType {
  DEFAULT,
  MESSAGING,
}

const Map<String, NotificationType> notificationType = {
  'default': NotificationType.DEFAULT,
  'messaging-service': NotificationType.MESSAGING,
};
