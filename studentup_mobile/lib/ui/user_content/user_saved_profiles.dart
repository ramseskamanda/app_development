import 'package:flutter/material.dart';

class UserSavedProfiles extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('Coming Soon!'),
      ),
    );
  }
}
